services:
  picobrain:
    platform: linux/amd64
    build: .
    image: picobrain:latest
    volumes:
      - ./data:/data
    ports:
      - "8080:8080"
    command: ["--db", "/data/brain.db", "--model-cache", "/data/models", "--port", "8080"]
    restart: unless-stopped
