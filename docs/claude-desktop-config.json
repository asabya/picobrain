{
  "mcpServers": {
    "picobrain": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-streamable-http",
        "http://localhost:8080",
        "--auth-header",
        "Bearer picobrain"
      ]
    }
  }
}
